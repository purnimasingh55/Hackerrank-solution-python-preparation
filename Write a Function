def is_leap(year):
    bool(year)
    if (( year%400 == 0)or (( year%4 == 0 ) and ( year%100 != 0))):
        return bool(True)
    else:
        return bool(False)
    return is_leap
year = int(input())
print(is_leap(year))
